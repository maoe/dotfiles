set commit-order = topo
set blame-options = -C -C -C
set blame-view = \
  id:yes,color date:default author:abbreviated line-number:yes text

bind main    R !git rebase -i %(commit)^
bind diff    R !git rebase -i %(commit)^
